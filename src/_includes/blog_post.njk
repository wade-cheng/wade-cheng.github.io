---
layout: "base.njk"
---

<div class="date">
    {{ page.date | formatDate }}{% for tag in tags -%} {% if tag !== 'blog-post'
    %} | #{{ tag }} {% endif %} {%- endfor %}
</div>

{{ content | safe }}

<p>
    <small><a href="/blog/">back to blog</a></small>
</p>
